---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  - name: fedora-21

suites:
  - name: default
    run_list:
      - recipe[socrates::default]
    attributes:

  - name: autochef
    run_list:
      - recipe[socrates::autochef]
    attributes:
